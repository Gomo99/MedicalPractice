namespace MedicalPractice.Status
{
    public enum NotificationCategory
    {
        General,       // default, fallback
        Appointment,
        Prescription,
        Billing,
        Patient,
        System,
        Emergency
    }
}